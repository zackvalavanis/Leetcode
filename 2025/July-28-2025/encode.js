var strs = ["we","say",":","yes"]


const encode = (strs) => {
  var encoded = (strs.map((word) => word.length + '#' + word).join(''))
  return encoded
}

let encoded = (encode(strs))

const decode = (encoded) => { 
  ecoded.filter((e) => )
}

console.log(decode(encoded))